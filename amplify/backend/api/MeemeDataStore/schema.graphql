# input AMPLIFY { globalAuthRule: AuthRule = { allow: public } } # FOR TESTING ONLY

type Meeme @model @auth(rules: [{ allow: ownerId }]) {
  id: ID!
  ownerId: String!
  timestamp: String!
  labels: [String!]!
  imageUrl: String!
  localImageUrl: String
}

input UpdateMeme {
  id: ID!
  localImageUrl: String
}
